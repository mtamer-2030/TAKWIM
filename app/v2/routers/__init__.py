"""مسارات الأستاذ مفكّكةً حسب المجال (routers فرعيّة) لسهولة الصيانة والاستدامة.

كلّ وحدةٍ تعرّف `router = APIRouter()` (بلا بادئة) يُضمّها `admin.py` تحت `/admin`.
تتشارك المساعدات عبر `app/v2/web.py` (`_ctx`/`require_admin`/`templates`) و`..database`.
السلوك والمسارات لا تتغيّر بالتفكيك.
"""
