# خارطة الطريق — PHILO-TECH v2 (التطوير والتنظيف)

وثيقة حيّة لتطوير النظام وتنظيف ديونه التقنية. مرتّبة حسب الأولوية والأثر.
كل بند مستقلّ يمكن تنفيذه على حدة بأمان (اختبار ثمّ التزام ثمّ دفع).

الحالة: v2 هو النظام الوحيد للتشغيل. 86 اختباراً يمرّ. الذكاء الاصطناعي محلّي
(Ollama) اختياريّ. لا يزال **أرشيف v1** في الكود لأنّ محرّكاته النقيّة مُعاد
استعمالها، ولأنّ اختبارات تغطّي طبقته القديمة.

---

## القسم أ — التنظيف (الديون التقنية)

### أ‑1. حذف طبقة v1 الويب (بحذر) — أولوية عالية
**الوضع:** v2 لا يستعمل واجهة v1 إطلاقاً، لكنّها ما زالت في الكود وتغطّيها اختبارات.

**قابل للحذف بأمان بعد نقل الاختبارات:**
- `app/main.py` (تطبيق v1)، `app/web.py`، `app/routes/` (student.py, teacher.py)
- `templates/teacher/*` كلّها، `templates/student/home.html`، `templates/base.html`
- وحدات v1 الخاصّة غير المُعاد استعمالها: `app/db.py`, `app/queries.py`,
  `app/reports.py`, `app/rosters.py`, `app/assessments.py`, `app/evening.py`,
  `app/codes.py`, `app/docx_template.py`، و`app/schema.sql` (بعد التأكّد أنّ سكربت
  الترحيل لم يعد يحتاجه).

**يجب الإبقاء عليه (محرّكات نقيّة يعيد v2 استعمالها):**
`app/validation.py`, `app/scoring.py`, `app/docx_import.py`, `app/constants.py`,
`app/qrcodes.py`, `app/netinfo.py`, `app/settings.py`, `app/normalize.py`.

**العائق:** اختبارات تعتمد على v1: `test_integration.py` (يستورد `app.main`)،
`test_codes`, `test_rosters`, `test_evening`, وجزء من `test_docx`.
**الخطوة الصحيحة:** أوّلاً نقل/إعادة كتابة ما يستحقّ من هذه الاختبارات إلى مسارات
v2، ثمّ حذف الطبقة القديمة مع اختباراتها، مع تشغيل كامل الاختبارات بعد كل حذف.

### أ‑2. تبسيط الإعداد (config)
- توحيد رسائل العناوين: العنوان يُكتشَف تلقائياً؛ إزالة كل أثر متبقٍّ لـ `192.168.1.50`
  من `README`/`setup.ps1` كقيمة إلزامية (تبقى مثالاً فقط). ✅ الكود يكتشف تلقائياً.
- قراءة `config.ini` صارت متسامحة مع التكرار. ✅ (مُنجز)
- إضافة تحقّق عند الإقلاع يطبع تحذيراً واضحاً إن كانت كلمة السرّ افتراضية.

### أ‑3. إزالة التكرار المفاهيميّ
- «سياقات التقويم» (`EvaluationEvent`) من الترحيل مقابل «التقاويم بالأسئلة»
  (`Quiz`) — توحيد العرض أو توضيح الفرق في الواجهة (صفحة «التقاويم» القديمة مقابل
  «التقاويم بالأسئلة»).
- `EssayExercise` (تمرين الإنشاء) غير مستعمل في واجهة v2 بعد — إمّا ربطه بالتقاويم
  أو إزالته.

---

## القسم ب — التطوير (ميزات)

### ب‑1. ربط خطط الذكاء الاصطناعي بالتقارير التراكمية — أولوية عالية
عرض `StudentReport.ai_intervention_plan` و`ClassReport` **داخل** صفحة التقرير
التراكميّ للتلميذ/القسم (`/admin/gradebook/...`)، فتجتمع النقط + الرادار + خطة
التدخّل في مكان واحد قابل للطباعة.

### ب‑2. تصدير التقارير
- تصدير التقرير التراكميّ (فردي/جماعي) إلى **PDF** (طباعة المتصفّح جاهزة؛ إضافة
  ورقة أنيقة) و**CSV/Excel** لدفتر النقط.
- تصدير بطاقات رموز الدخول (QR + الاسم) للطباعة والتوزيع (كانت في v1).

### ب‑3. النسخ الاحتياطي داخل الواجهة
زرّ «نسخة احتياطية الآن» في لوحة الأستاذ ينزّل `philotech.db` بطابع زمنيّ
(كان في v1). `setup.ps1` يجدول نسخاً يوميّاً على ويندوز.

### ب‑4. إدارة التقاويم والأسئلة
- تحرير/معاينة تقويم مستورد قبل النشر (تصحيح أخطاء الاستيراد).
- تصدير تقويم إلى JSON (رحلة ذهاب وإياب) — كانت في v1.
- بنك أسئلة لإعادة الاستعمال.

### ب‑5. تحسينات تجربة الأستاذ
- في «الجلسات»: إشعار بصريّ/صوتيّ خفيف عند تسليم جديد (اختياريّ).
- في «التصحيح»: تصفية «غير المصادَق فقط»، وحفظ تلقائيّ.

### ب‑6. تحسينات تجربة التلميذ
- استئناف تلقائيّ للإجابة بعد انقطاع (حفظ مسودّة محلّياً).
- عارض PDF/صور للنصوص المصوّرة داخل التقويم.

---

## القسم ج — الجودة والبنية

- **CI**: تشغيل `pytest` آليّاً عند كل دفع (GitHub Actions).
- **تغطية**: رفع تغطية مسارات v2 (admin/student) باختبارات تكامل مخصّصة (تحلّ محلّ
  اختبارات v1 عند حذفها).
- **توثيق**: تحديث `README` بعد كل حذف/ميزة؛ إبقاء هذا الملفّ محدّثاً.
- **الأداء**: `winloop` تلقائيّ على ويندوز (مُنجز)؛ فهارس قاعدة إضافية إن لزم عند
  الأفواج الكبيرة.

---

## مقترح التسلسل (مراحل صغيرة آمنة)

1. **ب‑1** ربط خطط الذكاء الاصطناعي بالتقرير التراكميّ (أثر عالٍ، مخاطرة منخفضة).
2. **ب‑3** زرّ النسخ الاحتياطي (حماية بيانات فوريّة).
3. **ب‑2** تصدير PDF/CSV للتقارير.
4. **أ‑1** حذف طبقة v1 الويب على دفعات (مع نقل الاختبارات) — بعد استقرار الميزات.
5. **ج** إضافة CI ورفع التغطية.
6. الباقي حسب الحاجة الميدانية.

> مبدأ ثابت لا يُخالَف في كل تطوير: التصحيح داخل القاعة يقينيّ، الذكاء الاصطناعي
> محلّيّ ومقترحٌ لا حاكم، والتلميذ لا يرى نقطة (رادار نوعيّ فقط). SQLite حصراً،
> كل الأصول محلّية بلا CDN، وتدهور لطيف في كل مكان.
