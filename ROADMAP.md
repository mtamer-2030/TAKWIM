# خارطة طريق PHILO-TECH v2 — وثيقة تنفيذ موجَّهة إلى Claude Code

> **ضع هذا الملفّ في جذر المشروع:** `C:\Users\HP\Desktop\work space\takwim\ROADMAP.md`
>
> **الحالة:** محرَّرة بعد تشخيص كامل للشيفرة (شتنبر 2026). المرحلة صفر (القراءة والجرد) **منجَزة** ونتائجها مثبَّتة في القسم ٣ أدناه؛ لا تُعِدها.

---

## ٠. اقرأ هذا أوّلاً — قبل أي أمر

### ٠-١ ترتيب القراءة الإلزامي

1. `CLAUDE.md` إن وُجد في الجذر — **هو دستور المشروع**، والشيفرة تحيل عليه بأقسام مرقّمة (`CLAUDE.md §5`, `§7`, `§13`, `§14`).
2. `README.md`.
3. هذه الوثيقة.

**عند أي تعارض بين `CLAUDE.md` وهذه الوثيقة: توقّف واسأل صاحب المشروع. لا ترجّح بنفسك.**

> ملاحظة: جرد الملفّات في القسم ٣ لم يشمل امتداد `.md`، فقد تجد ملفّات توثيق لم تُذكر فيه. اقرأها ولا تحذفها.

### ٠-٢ صاحب المشروع

أستاذ فلسفة بالتعليم الثانوي بالرباط (ثانوية ابن سينا)، 22 سنة تدريساً، يطوّر هذا النظام لأقسامه الخاصّة. ليس مهندس برمجيات محترفاً لكنّه يقرأ الشيفرة ويفهم القرارات المعمارية. يشتغل بالعربية.

**كيف يريد أن تُخاطبه:**
- بالعربية (المصطلحات التقنية بالإنجليزية بين قوسين عند الحاجة).
- **بلا مجاملة وبلا تهويل.** إن كان قراره أو البنية الحالية خاطئة، قل ذلك مباشرة واقترح البديل.
- **ميّز دائماً** بين ما قرأته في الشيفرة وما تفترضه.
- **لا تعلن إنجاز شيء لم تشغّله.** إن تعذّر الاختبار، صرّح بذلك بوضوح.

### ٠-٣ قواعد لا تُخالَف أبداً

| # | القاعدة |
|---|---|
| ق-١ | **صفر اتصال بالإنترنت وقت التشغيل.** أي نداء خارجي في مسار تشغيلي = عطب يُبلَّغ عنه ويُزال. لا CDN، لا خطوط عن بعد، لا تحقّق من التحديثات. كل الأصول محزومة في `static/vendor/` و`static/fonts/`. |
| ق-٢ | **الذكاء الاصطناعي لا يشتغل أثناء الحصّة إطلاقاً.** كل معالجة ذكية بعد إغلاق الجلسة. وكل مخرَج من النموذج يمرّ على الأستاذ للمصادقة قبل أن يُعرض على متعلّم أو يُعتمد في نقطة. |
| ق-٣ | **التصحيح داخل القاعة يقينيّ** (قواعد صريحة)، لا حكم نموذج. |
| ق-٤ | **المتعلّم لا يرى نقطة رقمية أبداً** — وصف نوعيّ وتغذية راجعة فقط. |
| ق-٥ | **خطوات صغيرة قابلة للمراجعة:** تغيير منطقي واحد في الالتزام الواحد، مع شرح «ما قبل / ما بعد». |
| ق-٦ | **لا إعادة كتابة من الصفر ولا إعادة هيكلة واسعة** دون طلب صريح من صاحب المشروع. |
| ق-٧ | **كل تغيير يمسّ القاعدة** يصاحبه هجرة Alembic صاعدة ونازلة (up/down) واختبار. |
| ق-٨ | **كل إصلاح عطب يصاحبه اختبار** يفشل قبل الإصلاح وينجح بعده. |
| ق-٩ | **التراخيص:** MIT / Apache-2.0 / BSD فقط عند الاقتباس من مستودعات مفتوحة. لا GPL/AGPL إلّا بموافقة صريحة. صرّح بترخيص كل مصدر تستعمله ووثّقه في تعليق فوق المقطع المقتبس. |
| ق-١٠ | **لا تبعيات ثقيلة ولا تحتاج إنترنت وقت التشغيل.** كل حزمة تُثبَّت مرّة وتشتغل بعدها دون اتصال. صرّح بحجم كل تبعية جديدة ومبرّرها. |
| ق-١١ | **لا تخترع معطى بيداغوجياً.** إن احتجت معلومة عن المنهاج المغربي أو عن طريقة الأستاذ في التقويم، اسأله. راجع مسرد القسم ٢. |
| ق-١٢ | **لا تحذف ملفّاً اعتماداً على تخمين.** شغّل أمر خريطة الاستيراد (القسم ٣-٤) وطبّق قاعدة القرار. |

### ٠-٤ سير العمل في كل بند

```
اقرأ البند ← اعرض خطّتك على صاحب المشروع ← انتظر موافقته ←
نفّذ ← شغّل معيار «منجَز» بالكامل ← اعرض المخرَج الفعلي (لا وصفه) ← التزم في git
```

**لا تبدأ بنداً جديداً قبل أن يُقفَل السابق بموافقة صريحة.**

### ٠-٥ اصطلاح الالتزام (commit)

- فرع لكل بند: `git checkout -b <معرّف-البند>` (مثال: `remove-v1`).
- وسم قبل أي بند خطر: `git tag pre-<معرّف-البند>`.
- رسالة الالتزام بالعربية، سطر أوّل ≤ 72 حرفاً، ثمّ فقرة «ما قبل / ما بعد».
- لا دمج في `main` قبل أن يمرّ معيار «منجَز» كاملاً.

---

## ١. النظام: ما هو، وعلى أي عتاد يعمل

نظام إدارة تعليمي **يعمل دون اتصال بالإنترنت** على شبكة محلّية، لأستاذ فلسفة وفق المنهاج المغربي. الأستاذ يفتح جلسة تقويم في القسم، والمتعلّمون يلجون بهواتفهم عبر رمز QR، يجيبون، فتُصحَّح الأسئلة المغلقة يقينيّاً في الحين، ويتابع الأستاذ التقدّم آنيّاً. بعد الحصّة تُولَّد تقارير وخطط تدخّل علاجي بمحرّك محلّي.

### ١-١ التقنيات المستعملة فعلاً (مقروءة في الشيفرة، لا مفترضة)

| الطبقة | التقنية |
|---|---|
| الخادم | Python + FastAPI + uvicorn (`main.py`, `run.py`, `loop="auto"` لأنّ uvloop لا يعمل على ويندوز) |
| القاعدة | SQLite ملفّ واحد `data/philotech.db` · SQLAlchemy 2.0 async + aiosqlite · وضع WAL |
| الهجرات | Alembic (`migrations/`) — **مع تعارض قائم، انظر ح-٤** |
| القوالب | Jinja2، `templates/v2/` |
| الواجهة | Tabler + HTMX + Alpine.js + Chart.js + pdf.js + Video.js — **كلّها محلّية** في `static/vendor/` |
| الاستيراد | python-docx · python-pptx · pdfplumber · pytesseract · pandas + openpyxl |
| الذكاء المحلّي | Ollama عبر httpx على `localhost:11434` |

### ١-٢ العتاد وقيود التشغيل — لا تخالفها في أي قرار معماري

| العنصر | القيمة (مؤكَّدة) |
|---|---|
| الخادم | HP ZBook 15 G3 · Windows 10 Pro · i7-6820HQ (4 أنوية/8 خيوط) · 32GB RAM · Quadro M2000M 4GB |
| تعريف الرسوميات | **582.78** (CUDA 13.0) — يستوفي شرط Ollama لبطاقات قدرة الحوسبة 5.0–6.2 (يتطلّب 570+) |
| البطاقة | **لا تدير الشاشة** (`nvidia-smi`: 0MiB / 4096MiB مستعملة) ← الـ4GB متاحة كاملة للنموذج |
| Ollama | مثبَّت · `qwen2.5:3b-instruct` (1.9GB) مسحوب |
| الشبكة | راوتر Huawei B310s-22 · DHCP من 192.168.1.100 إلى 192.168.1.200 · الخادم موصول بكابل إيثرنت |
| **سقف الراوتر** | **32 جهازاً لاسلكياً كحدّ أقصى · 802.11b/g/n على 2.4GHz فقط** (مواصفة الشركة الصانعة) |
| الحمولة المستهدَفة | **45 متعلّماً متزامناً** + مراقبة حيّة من حاسوب الأستاذ |
| الأقسام | 8 أقسام / 3 مستويات: TC1–TC3 · 1BAC1–1BAC3 · 2BAC1–2BAC2 · أرقام المتعلّمين 01–45 |
| أجهزة المتعلّمين | هواتف (واحتمال لوحات إن مُنعت الهواتف) · متصفّحات متفاوتة · ذاكرة محدودة |
| اللغة | عربية RTL كاملة: الواجهة والتقارير والتصدير |

> **حاجز صلب:** 45 > 32. الراوتر الحالي **لا يستطيع** خدمة القسم الكبير: المتعلّم رقم 33 لن يتّصل أصلاً. هذا ليس مشكلة أداء تُحلّ بالشيفرة. انظر البند ٤-ج.

### ١-٣ حقائق تغيّر حساب المخاطر

- **لا توجد أي بيانات متعلّمين حقيقية في النظام بعد.** `data/mihakk.db` (قاعدة v1) فارغة من الأجوبة.
- **v2 لم يخرج من طور التطوير** ولم يُستعمل أمام قسم حقيقي.

**ما يترتّب على ذلك:** التغييرات المدمّرة للمخطّط **مجّانية اليوم** (تُحلّ بحذف `data/philotech.db` وإعادة البناء) و**باهظة بعد أوّل حصّة**. لهذا رُتِّبت المراحل أدناه بحيث تُحسم البنية أوّلاً، خلافاً للترتيب المعتاد الذي يضع سلامة البيانات أوّلاً. **هذا الترتيب صالح ما دامت القاعدة فارغة. فور دخول أوّل قسم حقيقي، تنقلب الأولوية وتصبح المرحلة ٢ (سلامة البيانات) شرطاً لكل ما بعدها.**

---

## ٢. مسرد بيداغوجي إلزامي — لا تخترع فيه شيئاً

### ٢-١ المهارات الستّ (قرار الأستاذ، شتنبر 2026)

**موحَّدة عبر المستويات الثلاثة**، بهذا الترتيب:

| # | المهارة |
|---|---|
| ١ | صياغة الإشكال |
| ٢ | البنية المفاهيمية |
| ٣ | الأطروحة |
| ٤ | البنية الحجاجية |
| ٥ | المناقشة |
| ٦ | التركيب |

**التبس واحد لم يُحسم بعد:** هل «المناقشة» و«التركيب» مهارتان مستقلّتان (ستّ)، أم مهارة واحدة باسم مركّب «المناقشة والتركيب» (خمس)؟ **اسأل الأستاذ قبل البذر ولا تقرّر من عندك.**

هذه القائمة تحلّ محلّ ما هو في الشيفرة اليوم: `TargetSkill` في `app/models.py` بخمس قيم (إشكال · مفاهيم · أطروحة · بنية حجاجية · استنتاج) و`SKILLS` في `app/services/analytics.py`.

### ٢-٢ سياق الاستعمال

- المهارة السادسة (التركيب) تخصّ **التمارين**؛ أمّا الفروض المحروسة في الثانية باك فتُنجَز إنشاءً فلسفياً كاملاً يصحّحه الأستاذ بنفسه.
- الأستاذ يبني فروضه المحروسة على هذه المحاور نفسها.
- التقاويم نوعان: **تشخيصي** في بداية المقطع، و**تمارين تطبيقية** على تقنيات الكتابة والمضامين.
- الأسئلة **ليست ثابتة**: الأستاذ يزوّد النظام بأسئلة جديدة في كل مرّة، تختلف حسب المستوى والمضمون المدرَّس.

### ٢-٣ الهيكلة الديداكتيكية في النموذج

```
مستوى (Level) ← مجزوءة (Module) ← مفهوم (Concept، اختياري) ← محور (Axis)
                                                              ← نصّ فلسفي (PhilosophicalText)
                                                                  ← سؤال تحليل (AnalysisQuestion)
مستوى ← تمرين إنشاء (EssayExercise)
```

### ٢-٤ ترميز المتعلّمين

- رموز الأقسام: `TC1` `TC2` `TC3` · `1BAC1` `1BAC2` `1BAC3` · `2BAC1` `2BAC2`
- أرقام المتعلّمين: `01` إلى `45`
- الولوج: رمز مسار (Massar) أو رمز دخول محلّي + لاحقة عشوائية، **مع بوابة حضور** — لا حساب ولا كلمة سرّ ولا بريد.
- هدف الأستاذ المصرَّح به: **منع متعلّم من الإجابة مكان آخر**.

---

## ٣. نتائج التشخيص — الحالة المعروفة للنظام

### ٣-١ الاكتشاف البنيوي الأكبر: نظامان في شجرة واحدة

| النظام | الملفّات | الحالة |
|---|---|---|
| **v1 (مِحَكّ)** | `app/db.py` · `app/schema.sql` · `app/queries.py` · `app/rosters.py` · `app/routes/` · `app/evening.py` · `app/main.py` · `app/web.py` · `templates/teacher/` | `sqlite3` خام، بلا ORM، قاعدة `data/mihakk.db`. **غير مركَّب في `main.py` ← شيفرة ميتة** |
| **v2 (PHILO-TECH)** | `app/database.py` · `app/models.py` · `app/v2/` · `app/services/` · `templates/v2/` | SQLAlchemy async، قاعدة `data/philotech.db`. **هو النظام الشغّال** |

`main.py` في الجذر يركّب v2 فقط. لكنّ v2 لا يزال يستورد من وحدات مشتركة نشأت في v1 (`constants`, `normalize`, `codes`, `scoring`, `validation`).

### ٣-٢ الأعطاب الحرجة (مقروءة في الشيفرة، مرتّبة حسب الخطورة)

| الرمز | العطب | الموضع | الأثر |
|---|---|---|---|
| **ح-١** | **انتحال هوية أي متعلّم** | `app/v2/web.py::current_student_id` و`app/v2/student.py::login_confirm` | الكوكي `pt_student` هو **معرّف المتعلّم الخام غير الموقَّع**. تعديله في أدوات المطوّر = انتحال كامل. `httponly` لا يحمي من هذا. **يُبطل تلقائياً بوابة الحضور وقفل الجهاز** |
| **ح-٢** | **تسرّب الفرض قبل موعده** | `app/models.py::Quiz` (`published` افتراضه `True`، و`level_id`/`group_name` يقبلان NULL) + `app/v2/student.py::_homework_visible_to` و`take_quiz` | تقويم مستورَد بلا مستوى/فوج يصير منشوراً لكل المستويات الثمانية فوراً، بلا جلسة ولا حضور ولا قفل. و`take_quiz` لا يتحقّق من تطابق المستوى في المسار المنزلي |
| **ح-٣** | **النسخ الاحتياطي غير متّسق** | `setup.ps1` (المهمّة المجدولة 20:00) | `Copy-Item` على ملفّ SQLite حيّ مع WAL ← نسخة ممزّقة. لا فحص سلامة، لا تدوير، النسخة على القرص نفسه، ولا نسخة إن كان الحاسوب مطفأً. **والنسخة الصحيحة (`src.backup`) كانت في `app/routes/teacher.py::backup_db` غير المركَّب** |
| **ح-٤** | **انحراف مخطّط القاعدة** | `main.py::lifespan` (`create_all`) مقابل `migrations/` و`alembic upgrade head` | القاعدة تُنشأ بـ `create_all` دون ختم Alembic ← أوّل `upgrade head` يفشل أو يُطبَّق جزئياً |
| **ح-٥** | **إعادة التسليم تكتب فوق الجواب** | `app/v2/student.py::submit_quiz` و`take_quiz` | لا فحص لـ `part.submitted_at`. والمتعلّم يرى التغذية الراجعة فوراً (`reveal_feedback` افتراضه `True`) ← يرجع ويصحّح، ولا سجلّ للمحاولة الأولى. **يُفسد كل تشخيص تُبنى عليه خطط التدخّل** |
| **ح-٦** | **لا حفظ تدريجي** | نموذج `templates/v2/student/quiz.html` يُسلَّم مرّة واحدة | انقطاع الشبكة أو قفل الشاشة = ضياع كل ما كتبه المتعلّم. **و`README.md` يَعِد بـ«حفظ فوريّ واستئناف بعد الانقطاع» — هذا غير مطابق للشيفرة** |
| **ح-٧** | **نداء إنترنت حيّ** (خرق ق-١) | `app/evening.py` + قسم `[evening_ai]` في `app/settings.py` و`config.ini` | `httpx.post` نحو `https://api.anthropic.com` بمفتاح API في ملفّ نصّي صريح. معطَّل افتراضياً لكنّه حيّ في الإعداد |
| **ح-٨** | **كلمة السرّ الافتراضية تشتغل** | `app/settings.py::_read_ini` + `start.bat` | إن غاب `config.ini` **يُقرأ `config.ini.example`** ← `password = change-me-please` المنشورة. ولا حدّ لمحاولات الدخول، ولا HTTPS (كلمة السرّ والكوكي بالنصّ الصريح على الشبكة) |
| **ح-٩** | رموز الأستاذ في الذاكرة | `app/v2/web.py::_admin_tokens` و`app/v2/admin.py::_AI_JOBS` | إعادة تشغيل الخادم تُخرج الأستاذ وتُفقد شريط تقدّم التقارير |

### ٣-٣ مخاطر الحمولة عند 45 اتصالاً

1. **مجمّع الاتصالات — الاختناق الأرجح.** `app/database.py`: `create_async_engine(DATABASE_URL, echo=False, future=True)` **بلا ضبط للمجمّع** ← الافتراض `pool_size=5` + `max_overflow=10` = **15 اتصالاً متزامناً**، و`pool_timeout=30s`. عند 45 تسليماً متزامناً قد يسقط طلب بـ `TimeoutError`، أي **جواب متعلّم يضيع**. ⚠️ **لم يُقَس — يحتاج اختبار حمولة فعلياً.**
2. **كل مسار يفتح جلسته بنفسه** (`async with AsyncSessionLocal()` داخل الدوالّ) بدل تبعية `get_session` المعرّفة في `database.py` وغير المستعملة.
3. **حجم الاستجابة على الهاتف:** `templates/v2/student/base.html` يحمّل Chart.js وVideo.js وHTMX وAlpine في **كل** صفحة بما فيها الدخول، حيث لا يُستعمل أكثرها.
4. **ليس مشكلة:** الاستقصاء كل 3 ثوانٍ في `session_live.html` يأتي من **متصفّح واحد** (حاسوب الأستاذ) = 0.33 طلب/ث. لا تُضيّع وقتاً في «تحسينه».
5. **`synchronous=NORMAL` + WAL:** آمن ضدّ انهيار البرنامج، لكنّه يقبل فقدان آخر المعاملات عند انقطاع كهربائي مفاجئ. الخطر منخفض على حاسوب محمول ببطارية.

### ٣-٤ ما هو مصمَّم جيّداً — لا تعبث به

- ضبط PRAGMA في `app/database.py::_set_sqlite_pragmas`: WAL + `foreign_keys=ON` + `synchronous=NORMAL` + `busy_timeout=15000` على كل اتصال. **صحيح.**
- نداءات Ollama الحاجبة تُنفَّذ في `asyncio.to_thread` داخل `app/v2/admin.py::_run_ai_job` ← لا تجمّد حلقة الأحداث. **صحيح.**
- **التصحيح كلّه على الخادم:** `_raw_from_form` يعيد بناء الجواب من الحقول، و`grade_answer` يحتسب، وقواعد الفحص والنقاط الحاسمة تُنزَع قبل العرض (`safe_payload` / `safe_indicators`). المتعلّم لا يستقبل مفتاح التصحيح. **صحيح ومحكم.**
- كل الأصول محلّية، بلا CDN. **صحيح.**
- `_SKILL_BANDS` في `student.py` يحوّل المتوسّط إلى وصف نوعيّ ولون بلا رقم (ق-٤). **صحيح.**

---

## ٤. الجرد المؤكَّد وقاعدة القرار

### ٤-١ خريطة الاستيراد — نتيجة مثبَّتة

الوحدات التالية لا يستوردها إلّا v1 نفسه أو اختبارات وحدات محذوفة:
`db` · `queries` · `rosters` · `evening` · `assessments` · `reports` · `web` · `app/main`

**والاكتشاف الحاسم:** `app/services/quizzes.py:14` (شيفرة v2 حيّة، منها `grade_answer` المستدعاة عند كل تسليم) يستورد `from ..scoring import answer_text, score_answer`، و`:15` يستورد `from ..validation import validate_assessment`.

> **`app/scoring.py` و`app/validation.py` ليسا شيفرة v1. حذفهما يعطّل تصحيح كل جواب في النظام.**

### ٤-٢ قاعدة القرار لأي ملفّ مشكوك فيه

> ملفّ **يبقى** إن استورده أيّ ملفّ ناجٍ (`main.py` الجذر · `app/v2/*` · `app/services/*` · `models.py` · `database.py` · `settings.py` · `importer.py` · `docx_import.py` · `scoring.py` · `validation.py`).
> ملفّ **يُحذف** إن لم يستورده إلّا `app/routes/*` أو `app/main.py` أو `app/web.py` أو اختبارٌ لملفّ محذوف.
> **طبّق القاعدة متعدّية** (transitively): ما يستورده ملفّ ناجٍ ينجو بدوره.

### ٤-٣ أمر إعادة توليد خريطة الاستيراد (PowerShell)

```powershell
cd "C:\Users\HP\Desktop\work space\takwim"
Get-ChildItem -Recurse -Filter *.py |
  Where-Object { $_.FullName -notmatch '\\(\.venv|\.git|__pycache__)\\' } |
  Select-String -Pattern '^\s*(from\s+\.|from\s+app|import\s+app|from\s+main)' |
  ForEach-Object { "{0}:{1}: {2}" -f ($_.Path -replace [regex]::Escape("$PWD\"),''), $_.LineNumber, $_.Line.Trim() }
```

### ٤-٤ الأحكام النهائية

**يُحذف (≈3400 سطر):**

```
app/routes/               (__init__ · student.py · teacher.py — 1127 سطراً)
app/db.py  app/schema.sql  app/queries.py  app/rosters.py
app/main.py  app/web.py  app/evening.py
app/assessments.py        (لا يستورده إلّا routes/teacher.py:26)
app/reports.py            (لا يستورده إلّا routes/teacher.py:54 — بديله services/analytics.py + gradebook.py)
scripts/migrate_v1_to_v2.py
templates/teacher/        (20 ملفّاً، 910 أسطر)
templates/student/        (مجلّد v1 — فيه home.html وحده)
templates/base.html
tests/test_evening.py  tests/test_rosters.py  tests/test_integration.py
```

> ⚠️ **`templates/student/` هو مجلّد v1. لا علاقة له بـ `templates/v2/student/` الذي يجب أن يبقى كاملاً.** تحقّق من هذا السطر تحديداً قبل تنفيذه.

**يبقى قطعاً:**

```
app/scoring.py            ← services/quizzes.py:14
app/validation.py         ← services/quizzes.py:15 + docx_import.py:22   (ومعه tests/test_validation.py)
app/constants.py  app/normalize.py  app/codes.py  app/netinfo.py  app/qrcodes.py
app/importer.py  app/docx_import.py
tests/: test_scoring · test_codes · test_normalize · test_docx · test_validation
        test_analytics_ai · test_gradebook · test_importer · test_quizzes
```

**`app/docx_template.py` — استثناء مبرَّر:** بالقاعدة الحرفية يُحذف (لا يستورده إلّا `routes/teacher.py:36` و`tests/test_docx.py:8`). **لكنّه يبقى**، لأنّه يولّد قالب Word الذي يؤلّف به الأستاذ تمارينه، و`templates/v2/admin/import.html` يحيل على `samples/quiz_template.docx` بينما `app/v2/admin.py` **لا يستورد `build_template_docx` إطلاقاً** — أي أنّ v2 فقد هذه الميزة صامتاً. الملفّ مستقلّ ولا يستورد شيئاً من v1. **ربطه بمسار `/admin/quizzes/template` بندٌ لاحق (٥-د).**

---

## ٥. المراحل

> الترتيب مقصود ومبرَّر في §١-٣. لا تقفز بين المراحل.

---

### المرحلة ١ — حسم البنية (قبل أي شيء آخر)

#### البند ١-أ · حذف v1 — **مصادَق عليه، ابدأ به**

**الهدف:** نظام واحد لا نظامان. إسقاط ح-٧ نهائياً. تقليص مساحة الصيانة بنحو 3400 سطر.

**الخطوات بالترتيب:**

1. **شبكة الأمان.** `git status` (لا تتابع إن لم يكن نظيفاً) ← `git tag pre-v1-removal` ← `git checkout -b remove-v1`.
   **إن لم يكن المشروع تحت git: توقّف وأبلغ الأستاذ. لا حذف من مشروع بلا تاريخ.**
2. **الإنقاذ قبل الحذف.** أنشئ `app/backup.py` يحمل منطق النسخ الاحتياطي المتّسق المستخرَج من `app/routes/teacher.py::backup_db`: `sqlite3.Connection.backup` (لا نسخ ملفّ)، مع حذف الملفّ الناتج عند الفشل حتى لا يبقى نصف مكتوب. **لا تربطه بأي مسار في `/admin` الآن** — الزرّ والتدوير وفحص السلامة بند ٢-أ. يبقى هذا الالتزام «حذفاً + إنقاذاً» لا ميزة جديدة.
3. **الحذف** بـ `git rm` حسب قائمة §٤-٤.
4. **إعادة تشغيل أمر §٤-٣** والتحقّق من خلوّ المخرَج من أي استيراد نحو وحدة محذوفة.
5. **جراحة `app/settings.py`:** احذف الصنف `EveningAI` وحقوله · `s.evening = EveningAI(...)` وكتلة `if cp.has_section("evening_ai")` · وثوابت v1: `DATA_DIR` و`DB_PATH` (قاعدة mihakk) و`BACKUP_DIR`. ثمّ احذف قسم `[evening_ai]` من `config.ini` ومن `config.ini.example`.
6. **لا تلمس `app/constants.py`.** سيصير فيه ميت (`COMPETENCIES`, `KINDS`, `KIND_LABELS`, `DIAGNOSTIC_SEED`) لكنّه تنظيف منفصل (بند ١-د).

**معيار «منجَز»:**
- أمر §٤-٣ لا يعطي أي استيراد نحو وحدة محذوفة.
- `python -c "import main"` يمرّ.
- `python run.py` يقلع ويطبع لافتة العناوين.
- تستجيب: `/admin/login` · `/admin` · `/admin/qr.png` · `/student` · `/admin/import`.
- `pytest` أخضر بـ 9 ملفّات اختبار — **اعرض المخرَج الفعلي**.
- `git diff --stat`: حذف ≈3400 سطر + ملفّ واحد مضاف.

**مخاطر التراجع:** منخفضة. `git checkout pre-v1-removal` يعيد كل شيء. لا دمج في `main` قبل نجاح المعيار كاملاً.

**دَيْن يُسجَّل صراحةً:** حذف `tests/test_integration.py` يترك **v2 بلا أي اختبار شامل عبر HTTP**؛ تبقى اختبارات الوحدات فقط. يُسدّ في المرحلة ٣ والبند ٤-ب.

---

#### البند ١-ب · توحيد `Submission` و`QuizAnswer` — **مصادَق عليه**

**المشكلة:** مساران مستقلّان للنقطة في `app/models.py`:
- `Submission` — مرتبط بـ `EvaluationEvent` و`AnalysisQuestion`/`EssayExercise` والمهارات؛ تقرأ منه التقارير وتحليل المهارات.
- `QuizAnswer` — مرتبط بـ `QuizQuestion`؛ تكتب فيه واجهة المتعلّم وتقرأ منه لوحة التقاويم.

انشطار مفاهيمي لا مجرّد ازدواج: التقارير وخطط التدخّل لا ترى ما أجاب عليه المتعلّم في التقاويم.

**الاتجاه المقترح (غير مثبَّت):** الإبقاء على نموذج `QuizAnswer` — وهو الأغنى (`raw` JSON · `auto_score` · `manual_score` · `teacher_confirmed`) — وإلحاق `AnalysisQuestion` و`EssayExercise` بجدول الأسئلة الموحّد بدل جدول إنجازات منفصل.

**قبل أن تكتب سطراً:** اقرأ `app/services/analytics.py` و`app/services/gradebook.py` و`app/services/quizzes.py` **كاملة**، ثمّ اعرض تصميمك على الأستاذ. لا تنفّذ قبل موافقته.

**معيار «منجَز»:** جدول إنجازات واحد · التقارير الفردية والفصلية تُنتج نفس الأرقام على بيانات اختبار مبذورة · اختبار يثبت أنّ الجواب المغلق والمفتوح والإنشائي يدخلون المسار نفسه ويظهرون في ملفّ المهارات.

**مخاطر التراجع: الأعلى في الخارطة. لهذا موضعه الآن، والقاعدة فارغة — لا لاحقاً.**

---

#### البند ١-ج · المهارات الستّ الموحَّدة

**الهدف:** استبدال `TargetSkill` بالقائمة المعتمدة في §٢-١.

**نطاق أوسع ممّا يبدو — الملفّات المعنيّة:**
- `app/models.py` — `TargetSkill` و`_ar_enum`
- `app/services/analytics.py` — الثابت `SKILLS`
- `app/ai_feedback.py:11` — يستورد `SKILLS` من analytics، وصياغات الـ prompts مبنيّة على المهارات
- `templates/v2/student/_progress.html` (الرادار) وقوالب التقارير
- هجرة Alembic

**قراران معلّقان — اسأل الأستاذ:**
1. ستّ مهارات أم خمس؟ (§٢-١).
2. **جدول `skills` مبذور أم توسيع `Enum`؟** التوصية: **جدول**، لأنّ `_ar_enum` يفرض القيم كقيد على مستوى القاعدة، فكل تعديل مستقبلي يصير هجرة Alembic لا إدخالاً في لوحة الأستاذ — وقد غيّر هذه القائمة مرّة بالفعل. لكنّ التوحيد بين المستويات أسقط المبرّر الأقوى للجدول، فالقرار للأستاذ.

**معيار «منجَز»:** الرادار يعرض المهارات الستّ بتسمياتها الصحيحة · `TargetSkill` لم يعد موجوداً في الشيفرة · هجرة up/down تمرّان · اختبار يتحقّق من تسمية كل مهارة وترتيبها.

---

#### البند ١-د · تنظيف `app/constants.py`

إزالة ما صار ميتاً بعد ١-أ (`COMPETENCIES`, `KINDS`, `KIND_LABELS`, `DIAGNOSTIC_SEED` — تحقّق بأمر §٤-٣ قبل الحذف). التزام صغير مستقلّ.

---

### المرحلة ٢ — سلامة البيانات (قبل أوّل حصّة حقيقية، لا قبل ذلك)

#### البند ٢-أ · نسخ احتياطي يستحقّ اسمه (يعالج ح-٣)

على أساس `app/backup.py` المُنشأ في ١-أ، أضِف:
- `PRAGMA integrity_check` على النسخة بعد كتابتها، ورفض إعلان النجاح إن فشل.
- تدوير يُبقي آخر ١٤ يوماً.
- **نسخة تلقائية عند إغلاق كل جلسة صفّية** (لا مرّة يومياً فقط).
- زرّ «نسخة احتياطية الآن» في `/admin` يُنزّل الملفّ على المتصفّح.
- أمر واحد للنسخ إلى مفتاح USB (خارج قرص الحاسوب).
- تصحيح `setup.ps1`: استبدال `Copy-Item` بنداء سكربت النسخ.

**معيار «منجَز» — لا تسويف فيه:** تُؤخذ نسخة والخادم يشتغل و45 عميلاً وهميّاً يكتبون، ثمّ **تُفتح النسخة وتُقرأ ويُقارن عدد الأجوبة بالأصل**. **إن لم يُختبر الاسترجاع فلا نسخة احتياطية.**

#### البند ٢-ب · Alembic حصراً (يعالج ح-٤)

احذف `Base.metadata.create_all` من `main.py::lifespan` · هجرة أولى واحدة تمثّل المخطّط بعد المرحلة ١ · `alembic upgrade head` في `start.bat` · وثّق `alembic stamp` لأي قاعدة قائمة.

**معيار «منجَز»:** قاعدة جديدة تُبنى من صفر بـ Alembic وحده · `alembic downgrade -1` ثمّ `upgrade head` يمرّان بلا خطأ.

---

### المرحلة ٣ — النزاهة والأمن (قبل أوّل حصّة حقيقية)

| البند | العطب | الإصلاح |
|---|---|---|
| ٣-أ | ح-١ | كوكي موقَّع: `itsdangerous` (مرافقة لـ Starlette أصلاً — **بلا تبعية جديدة**) أو `SessionMiddleware` بسرّ من `config.ini`. |
| ٣-ب | ح-٢ | `published` افتراضه `False` · رفض حفظ تقويم بلا مستوى · التحقّق من المستوى داخل `take_quiz` لا في شرط العرض فقط. |
| ٣-ج | ح-٥ | رفض التسليم إذا كان `part.submitted_at` مضبوطاً · فكّ القفل من لوحة الأستاذ فقط · تسجيل كل محاولة بدل الكتابة فوقها. |
| ٣-د | ح-٨ | حذف قراءة `config.ini.example` كبديل عند غياب `config.ini` · رفض الإقلاع على `change-me-please` برسالة عربية واضحة · تخزين تجزئة لا نصّاً · تأخير تصاعدي على الدخول الفاشل. |
| ٣-هـ | ح-٩ | نقل رموز الأستاذ إلى كوكي موقَّع بدل مجموعة في الذاكرة. |

**معيار «منجَز» للحزمة (ق-٨):** اختبار آليّ لكل بند — وخاصّةً **اختبار يزوّر كوكي المتعلّم ويتوقّع 401**، واختبار يحاول تسليماً ثانياً ويتوقّع الرفض.

---

### المرحلة ٤ — ما ينكسر تحت الحمولة الحقيقية

- **٤-أ · ضبط المجمّع:** `pool_size` و`max_overflow` و`pool_timeout` صراحةً في `create_async_engine`، واستعمال تبعية `get_session` بدل فتح جلسة داخل كل دالّة.
- **٤-ب · اختبار حمولة 45 عميلاً:** سكربت يحاكي دخول 45 + تصفّح + تسليم متزامن. **المعيار الوحيد المقبول: وصول 45 جواباً إلى القاعدة، لا 43.** يُقاس زمن الاستجابة وتُسجَّل النتيجة.
- **٤-ج · الشبكة:** حاجز صلب لا شيفرة فيه. يحتاج نقطة ولوج تدعم 2.4+5GHz و802.11ac بحدّ عملاء ≥60. **في انتظار ذلك: جرّب في قسم ≤25 متعلّماً لاختبار النظام، لا لاختبار الراوتر.**
- **٤-د · تنحيف الواجهة:** Chart.js وVideo.js لا يُحمَّلان إلّا في الصفحات التي تستعملهما.

---

### المرحلة ٥ — إتمام المنجَز جزئياً

- **٥-أ · الحفظ التدريجي (ح-٦) — أولوية داخل هذه المرحلة.** حفظ كل جواب فور الإجابة عليه (HTMX متاح)، واستئناف بعد الانقطاع. يجعل وعد `README.md` صادقاً. **أهمّ من OCR لأنّه يمسّ بيانات المتعلّمين مباشرة.**
- **٥-ب · OCR.** الحالة اليوم: `app/importer.py::extract_text_from_image` يمرّر الصورة إلى `pytesseract` **كما هي** — بلا تصحيح ميل، بلا رمادي/عتبة، بلا ضبط DPI، بلا `--psm`. و`extract_text_from_pdf` يستعمل `pdfplumber` وحده، **فيعيد نصّاً فارغاً بصمت أمام PDF ممسوح ضوئياً** — وهو الحال الشائع مع صور الكتب المدرسية.
  **المطلوب:** معالجة قبلية + رجوع تلقائي إلى OCR عند عودة نصّ فارغ.
  **تطبيق ق-٩ هنا إلزامي:** ابحث في GitHub قبل كتابة أي مكوّن (Tesseract مع `ara` · PaddleOCR · Surya · أدوات تصحيح الميل والتحسين). قدّم **بديلين على الأقلّ** مع مقارنة موجزة (النضج · آخر تحديث · الحجم · ملاءمة العربية · الترخيص)، ثمّ وصِّ بواحد وعلّل. **لا تدّعِ أنّ مستودعاً يفعل شيئاً دون التحقّق من وثائقه أو شيفرته؛ وإن تعذّر التحقّق، قل ذلك.**
- **٥-ج · Ollama.** ثلاثة أعمال:
  1. **قياس فعليّ قبل أي قرار:** `qwen2.5:3b-instruct` على البطاقة مقابل نموذج 7–8B بتكميم Q4 على المعالج، بنصّ عربي حقيقي من فروض الأستاذ. سجّل رمزاً/ثانية وجودة المخرَج.
     *للسياق — تقديرات نظرية لم تُقَس:* 3B على البطاقة ≈ 15–25 رمز/ث · 7–8B على المعالج ≈ 4–6 رمز/ث. الحدّ: 4GB لا تسع أكثر من 3–4B. **الزمن ليس القيد (المعالجة بعد الحصّة)، بل جودة العربية.** انتبه أنّ العربية تستهلك ضعفين إلى ثلاثة أضعاف الرموز مقارنة بالإنجليزية، فسقف `num_predict: 350` في `app/ai_feedback.py::_generate` ≈ 120–180 كلمة عربية فقط.
  2. **تقييد المخرَج بمخطّط JSON** (`"format": "json"` في Ollama) بدل تفكيكه بـ regex. الحالة اليوم في `app/ai_feedback.py::suggest_open_score`: يبحث عن أوّل رقم في السطر — ويكفي أن يكتب النموذج «السقف 4» ليُسجَّل 4. **إصلاح صغير وأثره كبير.**
  3. **فرض ق-٢ برمجياً:** منع تشغيل `/admin/ai/run` وأي جلسة مفتوحة. اليوم هو عُرف لا قيد.
- **٥-د · إعادة ربط قالب Word:** مسار `/admin/quizzes/template` يستدعي `build_template_docx` من `app/docx_template.py` (الميزة ضاعت صامتاً في الانتقال إلى v2).

---

### المرحلة ٦ — تجربة المتعلّم على الهاتف

حجم مناطق اللمس · سلوك لوحة المفاتيح العربية · رسالة واضحة عند انقطاع الشبكة أثناء الإجابة · اختبار على متصفّحات وذاكرات متفاوتة.

### المرحلة ٧ — الميزات الجديدة

**لا شيء قبل ما سبق.**

---

## ٦. ما لا تفعله أبداً

- ❌ لا تحذف `app/scoring.py` ولا `app/validation.py` (`services/quizzes.py` يعتمد عليهما).
- ❌ لا تحذف `templates/v2/student/` — المقصود بالحذف `templates/student/` (مجلّد v1).
- ❌ لا تشغّل `scripts/migrate_v1_to_v2.py` — لا بيانات لترحيلها، وتشغيله يحقن كيانات وهميّة («مستوى مستورد» IMP · «مجزوءة عامة» · «تقويم مرحل من v1») في قاعدة نظيفة، **ويطوي تاريخ المتعلّم في إنجاز واحد مجمَّع بلا رجعة**. مصيره الحذف في ١-أ.
- ❌ لا تُدخل تبعية جديدة دون التصريح بحجمها ومبرّرها وترخيصها (ق-١٠).
- ❌ لا تُدخل أي نداء شبكة خارجي في مسار تشغيلي (ق-١).
- ❌ لا تُشغّل النموذج أثناء الحصّة، ولا تعرض مخرَجه على متعلّم قبل مصادقة الأستاذ (ق-٢).
- ❌ لا تعرض نقطة رقمية للمتعلّم (ق-٤).
- ❌ لا تُعِد هيكلة واسعة ولا تكتب النظام من الصفر (ق-٦).
- ❌ لا تعلن إنجاز بند لم تشغّل معيار «منجَز» الخاص به كاملاً.
- ❌ لا تخترع معطى بيداغوجياً — اسأل (ق-١١).

---

## ٧. الأسئلة المفتوحة — اسأل، لا تقرّر

1. ~~«المناقشة» و«التركيب»: مهارتان أم مهارة واحدة مركّبة؟~~ **حُسم:** مهارتان منفصلتان — ستّ مهارات موحّدة. (§٢-١)
2. ~~جدول `skills` مبذور أم توسيع `Enum`؟~~ **حُسم:** جدول `skills` مبذور (لا `Enum`). (١-ج)
3. ~~التصميم النهائي لتوحيد `Submission`/`QuizAnswer`~~ **حُسم:** جدول أجوبة واحد `answers` متعدّد الأشكال (يشير إلى سؤال تقويم أو سؤال تحليل نصّ أو تمرين إنشاء)؛ حُذف `Submission` و`EvaluationEvent` (بلا مسار كتابة في v2). (١-ب)
4. هل يستعمل الأستاذ قالب Word فعلاً؟ (٥-د — إن كان الجواب لا، يُحذف `app/docx_template.py`)
5. عند بلوغ ٥-ب: أي محرّك OCR بعد المقارنة؟
6. عند بلوغ ٥-ج: أي نموذج بعد القياس الفعلي؟

---

## ٨. لوحة الحالة — حدّثها بعد كل بند

| البند | الحالة | التاريخ | الالتزام |
|---|---|---|---|
| ١-أ حذف v1 | ✅ منجَز | 2026-09-11 | 33d04ac |
| ١-ب توحيد الإنجازات | ✅ منجَز (جدول answers موحّد؛ حُذف Submission/EvaluationEvent) | 2026-09-12 | e421799 |
| ١-ج المهارات الستّ | ✅ منجَز | 2026-09-11 | 857d87b |
| ١-د تنظيف constants | ✅ منجَز (KIND_LABELS + DIAGNOSTIC_SEED؛ COMPETENCIES/KINDS يبقيان — يستعملهما validation) | 2026-09-12 | 69aed43 |
| ٢-أ نسخ احتياطي | لم يبدأ | | |
| ٢-ب Alembic حصراً | لم يبدأ | | |
| ٣-أ … ٣-هـ الأمن | لم يبدأ | | |
| ٤-أ … ٤-د الحمولة | لم يبدأ | | |
| ٥-أ … ٥-د الإتمام | لم يبدأ | | |
| ٦ تجربة الهاتف | لم يبدأ | | |
| ٧ ميزات جديدة | مؤجَّل | | |
