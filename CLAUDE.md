# ذاكرة المشروع — PHILO-TECH (takwim)

نظام تقويم صفّيّ محلّيّ (LAN) لأستاذ فلسفة بالثانويّ المغربيّ: التلاميذ يجيبون على
هواتفهم عبر شبكة الراوتر، تصحيحٌ آنيّ للأسئلة المغلقة، وتصحيح آليّ بالذكاء للأسئلة
المفتوحة بانتظار مصادقة الأستاذ. الفرع النشط: `claude/new-session-zcmzcl`.

> هذا ملفّ الذاكرة الدائم: تقرؤه كلّ جلسة جديدة (تبدأ باردة). سجلّ الحالة التفصيليّ
> في `ROADMAP.md §8`. القرارات المعماريّة الملزِمة أدناه — لا تخالفها دون إذن صريح.

---

## قرارات معماريّة (2026-09-14)

### ١. الاستيراد: حتميّ أوّلاً، والسحابة للبنى الحرّة، والنموذج المحلّي خارج المسار
- **المشكلة الجذريّة المحسومة:** لا يوجد محلّل حتميّ (deterministic) يستوعب بنيات
  الفروض اللانهائيّة؛ ولا يقدر نموذج 3B محلّي على البطاقة الضعيفة (Quadro M2000M،
  4GB) على البنى الحرّة — بطيء (~٤ دقائق) ويُسقط النصّ.
- **القرار:** المحرّك المحلّي **ممنوعٌ من مسار الاستيراد نهائيّاً**. الاستيراد:
  1. **حتميّ فوريّ** (`heuristic_quiz_from_text`): عناوين/نصوص/اختيار/نقط، يحفظ النصّ
     الفلسفيّ (تُجمَع أسطر النثر المتتالية في `passage` واحد — إصلاح «نسي النص»).
  2. **سحابيّ عند التحضير** (`app/cloud_ai.py::extract_quiz_cloud` عبر Claude): يعالج
     **أيّ بنية** مع الأجوبة الصحيحة ومؤشّرات النجاح؛ يحتاج إنترنت ومفتاح API. الجلسة
     في القاعة تبقى **offline** تماماً.
  3. تدهور لطيف: بلا مفتاح/إنترنت → المسار الحتميّ. لا يضيع عمل الأستاذ أبداً.
- **دور النموذج المحلّي الوحيد:** تصحيح الأسئلة المفتوحة (اختياريّ)، لا الاستيراد.

### ٢. استقرار المخطّط: تحسين الاستيراد/الواجهة/التصحيح لا يستلزم ترحيلاً
- بنية البيانات (تلاميذ/أجوبة/تقاويم) **مُجمَّدة ومُدارة بـ Alembic** (المرحلتان ١–٢).
- تغييرات المحلّل/الواجهة/التصحيح تغيّر **كيف يصير الملفّ أسئلةً**، لا **شكل ما يُخزَّن**.
  ⇒ يُبدأ استعمال النظام الآن بأمان؛ التطوير اللاحق للاستيراد **بلا ترحيل معطيات**.

### ٣. حلقة التصحيح
- **مغلقة** (mcq_single/multi/classify/order): تصحيح آنيّ لحظة التسليم (صحيح=كامل).
- **مفتوحة** (long_text/short_text): مؤشّرات نجاح (`indicators`) تُخزَّن، يقترح المحرّك
  المحلّي نقطةً بها، **وتبقى بانتظار مصادقة الأستاذ**. قسم `/admin/grading`.
- **عرض فقط** (heading/passage): تُعرَض للتلميذ، لا تُرقَّم ولا تُصحَّح.
- التحليلات تستعمل `coalesce(manual,auto)` مع `eff IS NOT NULL` — لا تُصفَّر المصادَقة
  بلا نقطة أبداً.

### ٤. الشبكة (راوتر TP-Link Archer AX55 · WiFi 6 · يتّسع لقسم كامل)
- كشف IP متين عبر مختلف الراوترات (192.168.0.x/192.168.1.x/11.x/10.x) وبلا إنترنت:
  `app/netinfo.py` (عدّة بوّابات + `getaddrinfo`، يفضّل 192.168.*). أداة تشخيص للأستاذ:
  `scripts/netcheck.py`.
- نقاط فشل وصول الهواتف الشائعة (تشخيص، لا شيفرة): جدار حماية ويندوز (قاعدة حظر
  python تتغلّب على سماح المنفذ — يعالجها `scripts/fix_firewall.ps1`)، تصنيف الشبكة
  Public، عزل الأجهزة.
- **قراءة كلّ الهواتف** (السبب الجذريّ الأهمّ): الراوتر بلا إنترنت ⇒ الهاتف ذو 4G
  المشغّل يُغادر الشبكة تلقائياً فيفشل الوصول للـLAN. الحلّ (لا شيفرة، `docs/شبكة_القاعة.md`):
  (أ) القسم كلّه **«وضع الطيران ثمّ WiFi»** ← الأبسط بلا إنترنت؛ (ب) أو وصلة إنترنت
  للراوتر فتبقى الهواتف متّصلة. `run.py` يطبع العنوان + هذه التعليمة عند الإقلاع.

---

## ثوابت لا تُخالَف (Invariants)
- لا تُدخِل النموذج المحلّي (Ollama) في مسار الاستيراد.
- لا تحذف/تعطّل/تحجب سؤالاً للحصول على نتيجة؛ ولا تُصفّر جواباً مصادَقاً بلا نقطة.
- الجلسة في القاعة محلّية بحتة (LAN)؛ السحابة للتحضير فقط.
- التزم بأسلوب الشيفرة المحيط وبتعليقات عربيّة موجزة. لكلّ ميزة اختبارٌ يمرّ.
- كلّ تغيير: تحقّق (`python -m pytest`) ثمّ التزام برسالة عربيّة (ما قبل/ما بعد) وتحديث
  `ROADMAP.md §8`.

## ملفّات مفتاحيّة
- `app/services/quizzes.py` — المحلّل الحتميّ + إسناد عناصر الإجابة بالترقيم.
- `app/cloud_ai.py` — الاستيراد السحابيّ (Claude) لأيّ بنية.
- `app/ai_feedback.py` — المحرّك المحلّي (تصحيح مفتوح + توليد محلّي بديل).
- `app/v2/admin.py` — مسارات الاستيراد/المراجعة/التصحيح.
- `app/netinfo.py` + `scripts/netcheck.py` — كشف/تشخيص الشبكة.
- `config.ini` — `[local_ai]` (Ollama) و`[cloud_ai]` (مفتاح Claude/النموذج).

## الإعداد (config.ini)
- `[cloud_ai] api_key` أو متغيّر البيئة `ANTHROPIC_API_KEY`؛ `model` افتراضه
  `claude-sonnet-5` (اقتصاديّ للاستخراج). يتطلّب `pip install anthropic`.
- `[local_ai] model` افتراضه `qwen2.5:3b-instruct` (يناسب 4GB VRAM، 100% GPU).
